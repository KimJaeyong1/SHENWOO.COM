<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shenwoo.mapper.BoardMapper">
	<select id="getList1" resultType="com.shenwoo.domain.BoardVO">
		
		select * from board where bno > 0 and ntype = 1 order by bno desc
		
	</select>
	
	<select id="getList2" resultType="com.shenwoo.domain.BoardVO">
		
		select * from board where bno > 0 and ntype = 2 order by bno desc
		
	</select>
	
	<select id="getList3" resultType="com.shenwoo.domain.BoardVO">
		
		select * from board where bno > 0 and ntype = 3 order by bno desc
		
	</select>
	
	<select id="getList4" resultType="com.shenwoo.domain.BoardVO">
		
		select * from board where bno > 0 and ntype = 4 order by bno desc
		
	</select>
</mapper>